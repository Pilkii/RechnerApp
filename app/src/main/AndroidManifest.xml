<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="souraka.first.rechnerapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/africaicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/africaicon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Munzen" />
        <activity android:name=".Diffac" />
        <activity android:name=".MulAc" />
        <activity android:name=".MainActivity2" />
        <activity android:name=".Additionsac" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>